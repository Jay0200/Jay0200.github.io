var options = {
    strings: ["High School Grad", "Learner"],
    typeSpeed: 100,
    backSpeed: 10,
    backDelay: 1000,
    loop: true
  };
  
  var typed = new Typed('.multiple-filed', options);
  
