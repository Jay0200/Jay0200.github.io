var options = {
    strings: ["High School Graduate🎓", "Learner💡"],
    typeSpeed: 100,
    backSpeed: 10,
    backDelay: 1000,
    loop: true
  };
  
  var typed = new Typed('.multiple-filed', options);
  
